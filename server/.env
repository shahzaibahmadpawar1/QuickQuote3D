# MongoDB Connection String
# Replace <password> with your actual password and <cluster-url> with your Atlas address
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/quickquote3d?retryWrites=true&w=majority

# Optional: Port (default 5000)
PORT=5000

/** Authenticated app: floor planner + projects (path segment under `[locale]`). */
export const PLANNER_SEGMENT = 'planner'

/** Public read-only shared floorplan view. */
export const SHARE_SEGMENT = 'share'
